#![forbid(unsafe_code)]

#[allow(dead_code)]
#[path = "../static_elf_v2.rs"]
mod static_elf_v2;

fn main() {
    // Compile/test carrier only. T005-078 production dispatch is introduced separately.
}
