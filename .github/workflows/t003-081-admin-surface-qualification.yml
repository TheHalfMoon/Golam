name: t003-081-admin-surface-qualification

on:
  push:
    branches:
      - qualify/t003-081-admin-surface

permissions:
  contents: read

jobs:
  qualify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prove workflow registration
        run: echo T003-081 workflow registered
