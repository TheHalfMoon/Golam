name: spec005-h-skill-relay

on:
  push:
    branches:
      - impl/005-local-tools-context-memory
    paths:
      - .github/workflows/spec005-h-skill-relay.yml

permissions:
  contents: write

jobs:
  qualify-and-commit:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.98.0 --profile minimal --component rustfmt,clippy
      - name: Apply bounded skill package repairs
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/qualification/spec005_h_skill_repair.py
          python3 scripts/qualification/spec005_h_skill_repair_v2.py
          python3 scripts/qualification/spec005_h_skill_repair_v3.py
          cargo +1.98.0 fmt --all
          cargo +1.98.0 fmt --all -- --check
          cargo +1.98.0 clippy --locked -p golam-core -p golamd --all-targets -- -D warnings
          cargo +1.98.0 test --locked -p golam-core skills_protocol::
          cargo +1.98.0 test --locked -p golamd skill_packages::
          cargo +1.98.0 test --locked -p golamd --lib
      - name: Commit exact qualified skill package unit
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin impl/005-local-tools-context-memory
          test "$(git rev-parse origin/impl/005-local-tools-context-memory)" = "$GITHUB_SHA"
          git rm .github/workflows/spec005-h-skill-relay.yml scripts/qualification/spec005_h_skill_repair.py scripts/qualification/spec005_h_skill_repair_v2.py scripts/qualification/spec005_h_skill_repair_v3.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add crates/golam-core/src/skills_protocol.rs crates/golamd/src/lib.rs crates/golamd/src/skill_packages.rs
          git commit -m 'feat(005): govern skill package lifecycle'
          git push origin HEAD:impl/005-local-tools-context-memory
