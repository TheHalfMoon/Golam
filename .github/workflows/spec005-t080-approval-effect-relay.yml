name: spec005-t080-approval-effect-relay

on:
  push:
    branches:
      - impl/005-local-tools-context-memory
    paths:
      - .github/workflows/spec005-t080-approval-effect-relay.yml

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - run: echo relay-ok
